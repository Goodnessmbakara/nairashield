# URL of the NairaShield Cloudflare Worker.
# Local:  npx wrangler dev   -> http://127.0.0.1:8787
# Remote: npx wrangler deploy -> https://nairashield-bot.<subdomain>.workers.dev
PUBLIC_AGENT_URL=http://127.0.0.1:8787

# Optional: waitlist form endpoint
# PUBLIC_WAITLIST_URL=
