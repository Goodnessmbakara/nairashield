# klend-sdk peers expect @solana/kit ^2 while the project pins ^7 (runtime-compatible,
# fails closed in kamino.ts). Without this, npm install/ci refuse the tree that pnpm allows.
legacy-peer-deps=true
